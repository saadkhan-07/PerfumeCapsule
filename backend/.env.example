# ---------------------------------------------------------------------------
# Backend environment variable contract — Perfume Capsules
# This file IS committed. It documents required variables with NO real values.
# Copy to `.env` and fill in locally. See CLAUDE.md for details.
# ---------------------------------------------------------------------------

DATABASE_URL="postgresql://USER:PASSWORD@HOST:PORT/DATABASE?schema=public"

JWT_SECRET=
JWT_EXPIRES_IN="7d"

CLOUDINARY_API_KEY=
CLOUDINARY_API_SECRET=
CLOUDINARY_CLOUD_NAME=

NODE_ENV="development"
PORT="5000"

WHATSAPP_NUMBER=
