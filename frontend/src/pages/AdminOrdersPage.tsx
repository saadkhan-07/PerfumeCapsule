import { PageWrapper } from '../components/layout/PageWrapper'

export function AdminOrdersPage() {
  return (
    <PageWrapper>
      <h1 className="text-3xl font-semibold tracking-tight">Admin Orders</h1>
      <p className="mt-2 text-neutral-500">Phase 4 placeholder — manage orders (admin only).</p>
    </PageWrapper>
  )
}
